import type { CardId } from '../../game/domain/common'
import type { UnitInstance } from '../../game/domain/unit'
import { CardOption } from './CardOption'
import { ItemTargetPicker } from './ItemTargetPicker'
export function CardSelectionOverlay({offer,highlighted,itemTargeting,units,rerolls,onHighlight,onApply,onReroll,onClose,onEquip}:{offer:CardId[];highlighted?:CardId;itemTargeting:boolean;units:UnitInstance[];rerolls:number;onHighlight:(id:CardId)=>void;onApply:()=>void;onReroll:()=>void;onClose:()=>void;onEquip:(id:number)=>void}){return <div className="card-overlay" role="dialog" aria-label="라운드 카드 선택"><div className="card-overlay__scrim"/><section className="card-choice-panel"><header className="card-choice-header"><div><span className="eyebrow">ROUND 05 · CHOOSE ONE</span><h2>{itemTargeting?'장착 대상을 선택하십시오':'작전 카드를 선택하십시오'}</h2></div><button className="icon-button" onClick={onClose} aria-label="카드 선택 닫기">×</button></header>{itemTargeting?<ItemTargetPicker units={units} onPick={onEquip}/>:<><div className="card-grid">{offer.map(id=><CardOption key={id} id={id} selected={highlighted===id} onClick={()=>onHighlight(id)}/>)}</div><div className="reroll-row"><button className="reroll-button" onClick={onReroll} disabled={!rerolls}>↻<b>{String(rerolls).padStart(2,'0')}</b></button></div><div className="card-choice-footer"><span>선택 대기 중 · 제한 시간 없음</span><button className="primary-button" disabled={!highlighted} onClick={onApply}>카드 적용</button></div></>}</section></div>}
