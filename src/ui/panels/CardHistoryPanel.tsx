import type { CardRecord } from '../../game/domain/card'
import { cardDefinitions } from '../../game/config/card-definitions'
export function CardHistoryPanel({cards,rerolls}:{cards:CardRecord[];rerolls:number}){return <aside className="side-panel rule-history"><header className="panel-header panel-header--brand"><div className="brand-line"><span className="brand-mark">+</span><strong>DefencER</strong></div></header><div className="history-heading"><strong>카드 기록</strong></div><div className="rule-list">{cards.length===0?<p className="empty-history">아직 선택한 카드가 없습니다.</p>:cards.map((record,index)=>{const c=cardDefinitions[record.cardId];return <article className={`rule-card ${index===0?'rule-card--active':''}`} key={`${record.cardId}-${index}`}><div className="rule-card__icon">+</div><div className="rule-card__copy"><span className="rule-card__type">{c.kind} · ROUND {String(record.round).padStart(2,'0')}</span><strong>{c.title}</strong><p>{c.description}</p></div></article>})}</div><div className="left-status"><div className="status-row"><span>다음 카드 선택</span><strong>ROUND 05</strong></div><div className="status-row"><span>리롤 보유</span><strong className="status-row__gold">{String(rerolls).padStart(2,'0')}</strong></div></div></aside>}
