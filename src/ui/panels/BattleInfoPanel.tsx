import type { RunState } from '../../game/domain/run-state'
import { seconds } from '../shared/format'
export const BattleInfoPanel=({run,onStart,onSkip,onOpenCards}:{run:RunState;onStart:()=>void;onSkip:()=>void;onOpenCards:()=>void})=><section className="side-panel battle-info"><header className="panel-header panel-header--row"><div><strong>전투 정보</strong><span>BATTLE INFO</span></div></header><div className="round-readout"><div><span className="eyebrow">CURRENT ROUND</span><div className="round-number"><strong>{String(run.round.number).padStart(2,'0')}</strong><em>/ 10</em></div></div><div className="timer"><strong>{seconds(run.round.remaining)}</strong><span>{run.phase==='ready'?'전투 준비':run.phase==='card-selection'||run.phase==='item-targeting'?'카드 선택 중':'남은 시간'}</span></div></div>{run.phase==='ready'&&<button className="secondary-button panel-button" onClick={onStart}>전투 시작</button>}{run.phase==='combat'&&!run.enemies.length&&!run.pendingSpawns&&<button className="secondary-button panel-button" onClick={onSkip}>라운드 스킵</button>}{(run.phase==='card-selection'||run.phase==='item-targeting')&&<button className="secondary-button panel-button" onClick={onOpenCards}>카드 선택 열기</button>}</section>
